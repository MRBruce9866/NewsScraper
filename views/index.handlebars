{{!-- <div class="container-full">
    {{#each articles}}
    <div class="article m-3">
        <div class="row text-center headline">
            <div class="col-12">
                <a href="{{link}}"><strong>{{title.[0]}}</strong> {{title.[1]}} {{title.[2]}}</a>
            </div>
        </div>
        <div class="row m-4 content text-md-left text-center">
            <div class="col-lg-4 col-md-6 col-sm-12">
                <img src="{{image}}" alt="">
            </div>

            <div class="col-lg-8 col-md-6 col-sm-12">
                <p>{{snippet}}</p>
            </div>
        </div>
        <div class="row text-center">
            <div class="col-12">
                <input class="btn btn-primary" type="button" value="Input">
                <input class="btn btn-primary" type="button" value="Input">
            </div>
        </div>
    </div>

    {{/each}}
</div> --}}

<div class="container-full">
    <div class="row m-3">
        <div class="col-9">
            <div class="articleContainer">
                <div class="row">
                    {{!-- articles go here col-3 --}}

                    {{#each articles}}
                    <div class="col-3">
                        <a href="{{ link }}">
                        <div class="article m-3"
                            style="background: #100 url({{ image }}) no-repeat center; background-size: cover;">

                            <div class="row p-1">
                                <div class="offset-2 col-10 text-center">
                                    <div class="articleTitle mainTitle">{{ title.[0] }}</div>
                                    <div class="articleTitle subTitle">{{ title.[1] }}</div>
                                    <div class="articleTitle subTitle">{{ title.[2] }}</div>
                                </div>
                            </div>
                            <div class="row p-1">
                                <div class="col-12 text-center">
                                    <div class="articleSnippet">{{ snippet }} </div>
                                </div>
                            </div>

                            
                            <div class="articleStamp">TMZ</div>
                            <div class="barcodeStamp"></div>
                        </div>
                        </a>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
        <div class="col-3">
            <div class="noteContainer">
                <div class="row">
                    {{!-- notes go here col-3 --}}
                </div>
            </div>
        </div>
    </div>
</div>